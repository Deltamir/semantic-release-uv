{
  "name": "Node.js",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:4-22-bookworm",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.vscode-typescript-next", "vitest.explorer"]
    }
  },

  "postCreateCommand": {
    "yarn": "yarn install",
    "apt-get": "sudo apt-get update -qqy --no-install-recommends && sudo apt-get install pipx -qqy --no-install-recommends && pipx install pre-commit"
  },
  "postAttachCommand": {
    "pre-commit": "pre-commit install"
  }
}
